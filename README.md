Proyecto final de TEDS

### Setup rapido
- Crear entorno virtual: `python -m venv .venv` y `.venv\Scripts\activate`
- Instalar dependencias: `pip install -r requirements.txt`
- Ejecutar el servidor virtual: `python manage.py runserver`
